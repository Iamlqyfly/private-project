import React from 'react'
import {Router, Route, HashRouter} from 'react-router-dom'
import App from './App'

export default class WebappRouter extends React.Component{
  render() {
    return (
      <HashRouter>
        {/* <Route path='/' component={Container}></Route> */}
      </HashRouter>
    )
  }
}